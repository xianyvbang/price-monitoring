# NewAPI/Sub2API Account Grabber

独立于 `opencode-go-grabber` 的 Chrome/Edge Manifest V3 扩展，用于在 newApi / sub2Api 类网站侧边显示悬浮窗，并把已登录凭据导入余额监控 app。

请从 app 的“平台配置”页面下载定制后的 `account-grabber.zip`，不要直接加载源码目录。下载接口会把当前 app 部署域名写入扩展配置。
